import 'package:flutter/material.dart';

class DialKeypad extends StatelessWidget {
  const DialKeypad({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
